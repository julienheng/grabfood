

  <!DOCTYPE html>
<html lang="en">

  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Bootstrap 5 Search Example</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">

  </head>


  <div class="location-container">
    <div class="location-greeting">
      <p>Good Evening</p>
    </div>
    <div class="location-description">
      <p>Let's explore good food around you</p>
    </div>
    <%# <div class="search-bar">
      <input type="text" class="location-bar" placeholder="Search location">
      <button type="button" class="btn btn-success">Success</button>
    </div> %>
    <%# <div class="search-bar">
      <input type="text" class="search-dish" placeholder="search for dish or a restaurant">
      <button type="button" class="btn btn-success">Search</button>
    </div> %>

<%# <div class="d-flex align-items-center p10">
  <form class="d-flex">
    <input class="form-control me-1" type="search" placeholder="search for dish or a restaurant" aria-label="Search">
    <button class="btn btn-primary" type="submit">Search</button>
  </form>
</div> %>

<%= form_with url: root_path, method: :get, class: "d-flex search-bar" do %>
  <%= text_field_tag :query,
    params[:query],
    class: "form-control",
    placeholder: "Find Your Favourite Restaurant, Cuisine and Dish"
  %>
  <%= submit_tag "Search", class: "btn btn-success search-button" %>
  <% end %>
<br>


<div class="row">
  <% @restaurants.each do |restaurant|%>
    <div class="col-sm-3 d-flex" >
      <div class="card border p-3 border-success">
        <div class="card-img-top"><%= image_tag "fish.jpg" %></div>
        <div class="card-body d-flex flex-column">
          <div class="card-title"><h4><%= link_to restaurant.name, restaurant_path(restaurant) %></h4></div>
          <div class="card-subtitle mb-auto text-muted"><p><%= restaurant.category %></p></div>
          <div class="card-text mb-auto"><p><%= restaurant.location %></p></div>
          <div class="card-text mb-auto"><p>Opening Hour: <%= restaurant.opening_hour %></p></div>
          <div class="card-text mb-auto"><p>Closing Hour: <%= restaurant.closing_hour %></p></div>
        </div>
      </div>
    </div>
  <% end %>
</div>
