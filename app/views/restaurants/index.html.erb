<h1>There's always something for you</h1>
<h2>Let's explore some options</h2>
<p><%= link_to "Add a restaurant", new_restaurant_path(@restaurant) <p>

<div class="cards">
  <% @restaurants.each do |restaurant| %>
    <%= link_to restaurant.category, restaurant_path(@restaurant) %>
  <% end %>
</div>

<%= link_to "New restaurant", new_restaurant_path if policy(Restaurant).new? %>
